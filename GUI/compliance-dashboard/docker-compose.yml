version: '3.8'
services:
  app:
    build: .
    environment:
      - API_URL=${API_URL:-http://localhost:3000}
      - PORT=${PORT:-3000}
      - LOG_LEVEL=${LOG_LEVEL:-info}
    ports:
      - "3000:3000"
    restart: unless-stopped
